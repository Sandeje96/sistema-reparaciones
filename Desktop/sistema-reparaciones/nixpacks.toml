[phases.setup]
nixPkgs = ["python3", "pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python create_db.py"]

[start]
cmd = "gunicorn --bind 0.0.0.0:$PORT run:app"